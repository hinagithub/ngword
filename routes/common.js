const Ngword = require('../models').ngwords;
// TODO: シャッフルなどの共通処理はここに書くべき。

/*

common.js


書き方の例
const method = () => {
  // your method logic
}

const otherMethod = () => {
  // your method logic
}

module.exports = {
  method,
  otherMethod,
  // anotherMethod
};

呼び出す側の例
const common = require('./myModule.js');
const method = MyMethods.method;
const otherMethod = MyMethods.otherMethod;


*/
